name: API Pipeline

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Ruff
      - name: Install Ruff
        run: pip install ruff

      - name: Run Ruff
        run: ruff check .